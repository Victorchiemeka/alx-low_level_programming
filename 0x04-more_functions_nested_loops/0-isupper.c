#include "main.h"
#include <ctype.h>
/**
 * _isupper - a function that returns capital letters
 * @c: the parameter for the function
 * Return: 1 if condition is or 0 if false:
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
