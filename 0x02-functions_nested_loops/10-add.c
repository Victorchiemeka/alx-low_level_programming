#include "main.h"
/**
 * add - a function that adds two
 * @b: the parameter of the function
 * @a: the parameter:
 * Return: the value of compute
 */
int add(int b, int a)
{
	int compute;

	compute = a + b;
	return (compute);
}
