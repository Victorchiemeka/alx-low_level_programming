#include "main.h"
/**
 * add - a function that adds two
 * @int: the type of the returned value
 * Return: the value of compute
 */
int add(int b, int a)
{
	int compute;

	compute = a + b;
	return (compute);
}
