#include <string.h>
#include "main.h"
/**
 * _strcpy - a function that copies string
 * @dest: parameter to the fucnction
 * @src: parameter to the function
 * Return: returns dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
